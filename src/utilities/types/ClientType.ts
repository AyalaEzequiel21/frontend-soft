export type ClientType = {
    _id: string,
    nombre: string,
    telefono: string,
    balance: number,
    category: string,
    in_delivery: boolean,
    pagos: string[],
    ventas: string[]
  }